# -*- coding: utf-8 -*-
"""
Django settings for tiper_api project.
Generated by 'django-admin startproject' using Django 5.2.

💡 SHIN-VPS 統合開発環境用 最最適化版 (修正確定版)
- ローカル(HTTP)と本番(HTTPS)の両対応
- Next.js連携用 CORS/CSRF/Session 整合性
- WhiteNoiseによる静的ファイル配信
- 起動時の自動 collectstatic ロジック搭載
- マルチドメイン (*-host) 完全対応
"""

import os
import sys
from pathlib import Path

# プロジェクト内のパスを BASE_DIR / 'subdir' の形式で構築
BASE_DIR = Path(__file__).resolve().parent.parent

# ----------------------------------------------------
# 🔐 シークレット・デバッグ設定
# ----------------------------------------------------
SECRET_KEY = os.environ.get('DJANGO_SECRET_KEY', 'django-insecure-default-key-please-change-in-prod')

# 💡 ローカル開発時は True、本番・ステージングは環境変数 DEBUG=False を推奨
DEBUG = os.environ.get('DEBUG', 'True') == 'True'

# ----------------------------------------------------
# 🌐 ホスト / CORS / CSRF 設定 (環境対応)
# ----------------------------------------------------
ALLOWED_HOSTS = [
    # 本番・ステージングドメイン
    'stg.tiper.live', 'stg.bic-saving.com', 'stg.bicstation.com', 'stg.avflash.xyz', 'stg.blog.tiper.live',
    'tiper.live', 'bic-saving.com', 'bicstation.com', 'avflash.xyz', 'blog.tiper.live',
    # ローカル開発用カスタムホスト
    'api-tiper-host', 'tiper-host', 'bicstation-host', 'saving-host', 'avflash-host',
    'b-tiper-host', 'b-bicstation-host', 'b-saving-host',
    # Docker/標準ホスト
    'django-v2', 'localhost', '127.0.0.1', '*'
]

# CORS設定: Next.js(フロントエンド)からのアクセスを許可
CORS_ALLOWED_ORIGINS = [ 
    # 本番・ステージング
    "https://stg.tiper.live", "https://stg.bic-saving.com", "https://stg.bicstation.com",
    "https://stg.avflash.xyz", "https://stg.blog.tiper.live",
    "https://tiper.live", "https://bic-saving.com", "https://bicstation.com",
    "https://avflash.xyz", "https://blog.tiper.live",
    # ローカル開発用 (ポート8083: Traefik経由)
    "http://localhost:3000", 
    "http://localhost:8080", 
    "http://localhost:8083",
    "http://tiper-host:8083",
    "http://bicstation-host:8083",
    "http://saving-host:8083",
    "http://avflash-host:8083",
    "http://api-tiper-host:8083",
    "http://127.0.0.1:8083", 
    "http://host.docker.internal",
]

# 💡 重要: Cookieを使用した認証(NextAuth等)を許可
CORS_ALLOW_CREDENTIALS = True

# CSRFトークンを信頼するオリジン
CSRF_TRUSTED_ORIGINS = [
    'https://stg.tiper.live', 'https://stg.bic-saving.com', 'https://stg.bicstation.com',
    'https://stg.avflash.xyz', 'https://stg.blog.tiper.live',
    'https://tiper.live', 'https://bic-saving.com', 'https://bicstation.com',
    'https://avflash.xyz', 'https://blog.tiper.live',
    # ローカル開発用
    'http://localhost:3000', 
    'http://localhost:8083', 
    'http://tiper-host:8083',
    'http://api-tiper-host:8083',
    'http://127.0.0.1:8083', 
    'http://host.docker.internal', 
]

# ----------------------------------------------------
# 🍪 セッション・Cookie設定 (認証維持の要)
# ----------------------------------------------------
# 本番(HTTPS)なら Secure=True、ローカル(HTTP)なら False に自動切替
SESSION_COOKIE_SECURE = not DEBUG
CSRF_COOKIE_SECURE = not DEBUG

# Next.jsとの通信を許可するための設定
SESSION_COOKIE_SAMESITE = 'Lax'
CSRF_COOKIE_SAMESITE = 'Lax'
SESSION_COOKIE_HTTPONLY = True

# ----------------------------------------------------
# アプリケーション定義
# ----------------------------------------------------
INSTALLED_APPS = [
    'corsheaders', 
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'whitenoise.runserver_nostatic', # 開発サーバーでもWhiteNoiseを使用
    'django.contrib.staticfiles',
    'django_filters', 
    'rest_framework', 
    'django_extensions',
    'scrapers', 
    'api.apps.ApiConfig',
]

MIDDLEWARE = [
    'corsheaders.middleware.CorsMiddleware', # 最上部を推奨
    'django.middleware.security.SecurityMiddleware',
    'whitenoise.middleware.WhiteNoiseMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    # 自作ミドルウェアを追加
    'api.middleware.DomainDiscoveryMiddleware',
]

ROOT_URLCONF = 'tiper_api.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / 'templates'], 
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'tiper_api.wsgi.application'

# ----------------------------------------------------
# 🗄️ データベース設定 (PostgreSQL)
# ----------------------------------------------------
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': os.environ.get('DB_NAME', 'tiper_db'),
        'USER': os.environ.get('DB_USER', 'tiper_user'),
        'PASSWORD': os.environ.get('DB_PASSWORD', '1492nabe'),
        'HOST': os.environ.get('DB_HOST', 'postgres-db-v2'),
        'PORT': os.environ.get('DB_PORT', '5432'),
    }
}

# パスワード検証
AUTH_PASSWORD_VALIDATORS = [
    {'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',},
    {'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',},
    {'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',},
    {'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',},
]

# ----------------------------------------------------
# 👥 ユーザー認証設定 (✨ カスタムユーザーを適用)
# ----------------------------------------------------
AUTH_USER_MODEL = 'api.User'

# ----------------------------------------------------
# 🌍 国際化
# ----------------------------------------------------
LANGUAGE_CODE = 'ja'
TIME_ZONE = 'Asia/Tokyo'
USE_I18N = True
USE_TZ = True

# ----------------------------------------------------
# 📁 静的ファイル (WhiteNoise / STORAGES)
# ----------------------------------------------------
STATIC_URL = 'static/'
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')

STORAGES = {
    "staticfiles": {
        "BACKEND": "whitenoise.storage.CompressedManifestStaticFilesStorage",
    },
}

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# ----------------------------------------------------
# 🔌 外部 API 設定
# ----------------------------------------------------
API_CONFIG = {
    'DUGA': {
        'API_ID': os.environ.get('DUGA_API_ID', 'dummy_duga_id'),
        'API_KEY': os.environ.get('DUGA_AFFILIATE_ID', 'dummy_aff_id'), 
        'API_URL': os.environ.get('DUGA_API_URL', 'http://affapi.duga.jp/search'),
        'TOTAL_LIMIT': int(os.environ.get('DUGA_TOTAL_LIMIT', 1000)),
    },   
    'FANZA': {
        'API_ID': os.environ.get('FANZA_API_ID', 'dummy_fanza_id'),
        'API_KEY': os.environ.get('FANZA_AFFILIATE_ID', 'dummy_fanza_aff_id'),
        'API_URL': os.environ.get('FANZA_API_URL', 'https://api.dmm.com/affiliate/v3/ItemList'),
        'TOTAL_LIMIT': int(os.environ.get('FANZA_TOTAL_LIMIT', 1000)),
    },
}

# ----------------------------------------------------
# 🛠️ Django REST Framework (DRF) 設定
# ----------------------------------------------------
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'rest_framework.authentication.SessionAuthentication',
        'rest_framework.authentication.BasicAuthentication',
    ),
    'DEFAULT_PERMISSION_CLASSES': (
        'rest_framework.permissions.AllowAny', # 基本公開、必要なViewで制限
    ),
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.LimitOffsetPagination',
    'PAGE_SIZE': 10, 
    'DEFAULT_FILTER_BACKENDS': (
        'django_filters.rest_framework.DjangoFilterBackend',
        'rest_framework.filters.OrderingFilter',
        'rest_framework.filters.SearchFilter',
    ),
}

# ----------------------------------------------------
# 📝 ロギング設定
# ----------------------------------------------------
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'formatters': {
        'api_formatter': {
            'format': '[{levelname}] {asctime} ({name}): {message}',
            'style': '{', 'datefmt': '%H:%M:%S',
        },
    },
    'handlers': {
        'console': { 'class': 'logging.StreamHandler', 'formatter': 'api_formatter' },
    },
    'root': { 'handlers': ['console'], 'level': 'WARNING' },
    'loggers': {
        'django': { 'handlers': ['console'], 'level': 'INFO', 'propagate': False },
        'api': { 'handlers': ['console'], 'level': 'DEBUG', 'propagate': False },
    }
}

# ----------------------------------------------------
# 🚀 自動 collectstatic 実行ロジック
# ----------------------------------------------------
if DEBUG and 'collectstatic' not in sys.argv:
    # サーバー起動時のみ実行
    if 'runserver' in sys.argv:
        import subprocess
        if not os.path.exists(STATIC_ROOT) or not os.listdir(STATIC_ROOT):
            print("🛠️ [AUTO-INFO] staticfiles not found. Running collectstatic...")
            try:
                subprocess.run([sys.executable, "manage.py", "collectstatic", "--noinput"], check=True)
                print("✅ [AUTO-INFO] collectstatic completed.")
            except Exception as e:
                print(f"⚠️ [AUTO-ERROR] Failed: {e}")
                
# デフォルトの1000から、余裕を持った数値（例: 5000〜10000）に変更
DATA_UPLOAD_MAX_NUMBER_FIELDS = 10000