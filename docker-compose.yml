# docker-compose.yml

# サービス定義
services:

  # 1. PostgreSQL データベース (Django専用)
  postgres_db:
    image: postgres:15-alpine
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - internal-net
      
    # --- 💡 ヘルスチェック: 正常動作を確認済み ---
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"] 
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s 
    # ----------------------------------------------------

  # 4. Django API (データハブ & 認証サーバー)
  django:
    build:
      context: ./django
      dockerfile: Dockerfile
    container_name: api_django
    restart: always
    depends_on:
      postgres_db:
        condition: service_healthy
    environment:
      # 💡 修正: すべての環境変数をリスト形式に統一し、インデントを修正
      - DB_HOST=postgres_db
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY}  # SECRET_KEYをDJANGO_SECRET_KEYに変更
      
      # API設定
      - DUGA_API_ID=${DUGA_API_ID}
      - DUGA_AFFILIATE_ID=${DUGA_AFFILIATE_ID}
      - DUGA_API_URL=${DUGA_API_URL}
      - DUGA_TOTAL_LIMIT=${DUGA_TOTAL_LIMIT}
      
      - FANZA_API_ID=${FANZA_API_ID}
      - FANZA_AFFILIATE_ID=${FANZA_AFFILIATE_ID}
      - FANZA_API_URL=${FANZA_API_URL}
      - FANZA_TOTAL_LIMIT=${FANZA_TOTAL_LIMIT}
      # ----------------------------------------
      
    # ★ 静的ファイル用ボリュームを追加
    volumes:
      - ./django:/usr/src/app 
      - static_volume:/usr/src/app/staticfiles # ✅ 静的ファイル収集先をボリュームにマウント
        
    command: gunicorn tiper_api.wsgi:application --bind 0.0.0.0:8000
    networks:
      - internal-net

  # 5. Next.js - bicstation.com (ルートドメイン)
  next-bicstation:
    build:
      context: ./next-bicstation
      dockerfile: Dockerfile
      # --- 💡 修正: .env変数からタイトルを読み込む ---
      args:
        NEXT_PUBLIC_APP_TITLE: ${TITLE_BICSTATION}
      # ----------------------------------------
    container_name: next_bicstation
    depends_on:
      django:
        condition: service_started
    environment:
      NEXT_PUBLIC_API_URL: http://django:8000
      NODE_ENV: production
    networks:
      - internal-net

  # 6. Next.js - tiper.live (既存メインサイト)
  next-tiper:
    build:
      context: ./next-tiper
      dockerfile: Dockerfile
      # --- 💡 修正: .env変数からタイトルを読み込む ---
      args:
        NEXT_PUBLIC_APP_TITLE: ${TITLE_TIPER}
      # ----------------------------------------
    container_name: next_tiper
    depends_on:
      django:
        condition: service_started
    environment:
      NEXT_PUBLIC_API_URL: http://django:8000
      NODE_ENV: production
    networks:
      - internal-net
  
  # 7. Next.js - next-bic-saving サービス
  next-bic-saving:
    build: 
      context: ./next-bic-saving
      dockerfile: Dockerfile
      # --- 💡 修正: .env変数からタイトルを読み込む ---
      args:
        NEXT_PUBLIC_APP_TITLE: ${TITLE_SAVING}
      # ----------------------------------------
    container_name: next_bic_saving 
    depends_on:
      django:
        condition: service_started
    environment:
      # 💡 修正: API URLを追加
      NEXT_PUBLIC_API_URL: http://django:8000
      NODE_ENV: production
    networks:
      - internal-net

  # 8. Nginx (リバースプロキシ) - ローカルテスト用
  nginx:
    image: nginx:stable-alpine
    container_name: reverse_proxy
    depends_on:
      - next-bicstation
      - next-tiper
      - next-bic-saving
      - django 
    ports:
      - "8080:80" 
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro 
      - static_volume:/usr/src/app/staticfiles:ro # ✅ Nginxに静的ファイル配信用のボリュームをマウント
    networks:
      - internal-net

# ボリューム定義 (最上位レベル - services, networks と同階層)
volumes:
  postgres_data:
    driver: local
  static_volume: 
    driver: local

# ネットワーク定義 (最上位レベル)
networks:
  internal-net:
    driver: bridge