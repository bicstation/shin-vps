{
  "extends": "../shared/tsconfig.base.json",  // 👈 ここも ../ を追加
  "compilerOptions": {
    "baseUrl": ".",
    "jsx": "react-jsx",                       // 👈 React 18/19 の型エラーを消す魔法
    "paths": {
      "@/*": ["./src/*", "./*"],
      /* ✅ 修正の要: 
         shared は自分のフォルダ内ではなく、一つ上の階層 (../shared) にあります。
      */
      "@shared/*": ["../shared/*"],
      "@/shared/*": ["../shared/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "../shared/**/*.ts",   // 👈 ../ を追加して、一個上の shared を含める
    "../shared/**/*.tsx",  // 👈 ../ を追加
    "../shared/**/*.css"   // 👈 ../ を追加
  ],
  "exclude": ["node_modules"]
}