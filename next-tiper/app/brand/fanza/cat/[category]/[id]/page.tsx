/* /app/brand/fanza/cat/[category]/[id]/page.tsx */
import React from 'react';

/**
 * 🚧 一時的に空の状態でビルドを通すためのページコンポーネント
 */
export default async function FanzaCategoryPage() {
    return (
        <div style={{ padding: '40px', color: '#64748b', fontFamily: 'monospace' }}>
            <h2>FANZA_CAT_NODE_STUB</h2>
            <p>This path is currently under maintenance. Build dependency cleared.</p>
        </div>
    );
}