■ semantic attribute schema 方針確認結果（報告）

■ 結論

semantic attribute を
単なるタグではなく

商品意味レイヤ

として統一運用する方針で整理完了

■ ① attribute schema 正式方針

推奨正式仕様：

{
  id: number,
  slug: string,
  name: string,
  type: string,

  priority?: number,
  score?: number,

  icon?: string,
  color?: string,

  keywords?: string[]
}
補足
id

保持推奨

用途：

・DB参照
・React key
・キャッシュ安定化

ただし：

UIロジックは id 非依存
slug

semantic identifier として保持

用途：

・routing
・SEO
・semantic ID

type

最重要レイヤ

frontend は：

slug依存
↓
type依存

へ移行推奨

keywords

基本：

内部semantic engine専用

として扱う想定

■ ② type 正式方針

現時点定義：

gpu
cpu
maker
usage
memory
storage
feature
enum固定について

固定ではなく：

拡張型

を推奨

将来的追加候補
display
battery
cooling
material
os

など

■ ③ type別 engine 分離

結論：

type別 semantic engine 分離推奨
現在構成
keyword engine
gpu
cpu
maker
usage
numeric engine
memory
storage
将来的展開
score engine
embedding engine
hybrid semantic engine

へ拡張可能

■ ④ usage の正式役割

結論：

semantic intent layer

として扱う

usage はカテゴリではない

usage は：

何に使うPCか

を表す意味レイヤ

frontend想定
gaming
creator
mobile
ai
office

などを：

UX semantic

として利用

■ ⑤ slug の役割整理

結論：

slug依存最小化

推奨

slug用途
routing
SEO
semantic ID
UI制御

frontend は：

typeベースUI

へ移行推奨

■ ⑥ semantic attribute 共通基盤化

結論：

Finder
Ranking
Related
Recommendation

すべて：

semantic attribute layer

を共通参照する構成が理想

■ 現在のbackend状態

TSV-driven semantic engine 化済

構成：

TSV
↓
DB sync
↓
semantic matcher
↓
usage scoring
↓
attribute attach
■ 現在の属性付与状況
gpu:     2006
cpu:     2006
usage:   2006
maker:   1886
memory:   831
storage:  802
■ 意味

semantic engine が
実運用可能段階へ到達

■ frontend 推奨方針
・type-driven UI
・semantic usage
・attribute-driven routing
・slug依存削減
■ 総評

backend semantic engine と
frontend semantic UI を

同一意味体系

として統合可能な状態へ到達

以上