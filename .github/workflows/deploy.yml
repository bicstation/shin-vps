name: Production Deployment

on:
  push:
    # v1.0.0 などのタグがプッシュされた時だけ動作
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 1. ファイルを VPS のディレクトリへ同期
      # 🛠 修正点: .git フォルダを除外して転送エラーと速度低下を防止
      - name: Sync Files to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          port: 2222
          source: ".,!.git" # ここが重要！全ファイル対象だが .git は除外
          target: "/home/maya/shin-vps"
          strip_components: 0
          overwrite: true
          # 以前の権限エラーが残っている場合に備え、VPS側の同期先を一度整理する場合は rm: false（デフォルト）
          # もし権限エラーが消えない場合はここを true にして再試行してください

      # 2. SSH経由でネットワーク構築・Docker起動・DBマイグレーションを実行
      - name: Deploy to Production via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          port: 2222
          script: |
            set -e
            cd /home/maya/shin-vps
            
            echo "🔧 ネットワーク環境を整備します..."
            if docker network inspect shin-vps_shared-proxy >/dev/null 2>&1; then
                echo "既存のネットワークをクリーンアップします..."
                docker network rm shin-vps_shared-proxy || true
            fi
            docker network create shin-vps_shared-proxy || echo "Network already exists."

            echo "🚀 本番環境(prod)をビルド・起動します..."
            # --wait フラグを使うことで、コンテナがReady状態になるまで待機します
            docker compose -p shin-vps -f docker-compose.prod.yml up -d --build --remove-orphans --wait
            
            echo "📦 データベースを更新します..."
            # 構造変更がない場合でも「No migrations to apply」と出るだけで安全です
            docker compose -p shin-vps -f docker-compose.prod.yml exec -T django-v2 python manage.py migrate --noinput

            echo "🎨 静的ファイルを収集します..."
            # --clear を付けることで、前回のビルド時の古い静的ファイルの不整合を解消します
            docker compose -p shin-vps -f docker-compose.prod.yml exec -T django-v2 python manage.py collectstatic --noinput --clear

            echo "🧹 不要な資産を掃除します..."
            docker image prune -f
            
            echo "✅ デプロイ成功！全ての処理が完了しました。"