# =====================================================================
# 💡 SHIN-VPS Django Backend Requirements
# =====================================================================

# ------------------------------------------------------------------
# 🐍 Django コア & WSGI サーバー (Core & WSGI Server)
# ------------------------------------------------------------------
Django==4.2.1
gunicorn==20.1.0

# ------------------------------------------------------------------
# 🗄️ データベース・ドライバ (Database Driver)
# ------------------------------------------------------------------
psycopg2-binary==2.9.9

# ------------------------------------------------------------------
# 🔑 Django REST Framework (DRF) / 認証 / CORS
# ------------------------------------------------------------------
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
django-extensions==3.2.3

# ------------------------------------------------------------------
# 🔍 フィルタリング・検索機能 (Filtering)
# 💡 APIでの抽出・絞り込み機能に必須
# ------------------------------------------------------------------
django-filter==23.5

# ------------------------------------------------------------------
# ⚙️ 環境変数 & データ処理 (Env Vars & Data Processing)
# ------------------------------------------------------------------
python-dotenv==1.0.1
tqdm==4.66.1

# ------------------------------------------------------------------
# 🌐 API 通信 & HTTP クライアント (API & HTTP Clients)
# ------------------------------------------------------------------
requests==2.31.0
urllib3==2.1.0

# ------------------------------------------------------------------
# 📄 静的ファイル管理 (Static Files)
# ------------------------------------------------------------------
whitenoise==6.5.0

# ------------------------------------------------------------------
# 🕷️ スクレイピング & 自動化 (Scraping & Automation)
# ------------------------------------------------------------------
selenium==4.16.0
webdriver-manager==4.0.1
# WebページのHTML構造を解析し、本文を抽出するために使用
beautifulsoup4==4.12.2
# 文字コード判定（海外サイトの文字化け防止）
chardet==5.2.0
# RSSフィード（PC Watch等）を解析するために使用
feedparser==6.0.10

# ------------------------------------------------------------------
# 🖼️ 画像処理 (Image Processing)
# 💡 アイキャッチ画像の加工などが必要な場合に備えて
# ------------------------------------------------------------------
Pillow==10.1.0