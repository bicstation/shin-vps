# ファイル名: .github/workflows/deploy_prod.yml (最終版)

name: Production Deployment

on:
  # 本番環境へのデプロイは慎重に行うため、ステージングと同じく main ブランチへのプッシュをトリガーとします。
  push:
    branches:
      - main 

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production # 📘 環境名を指定することで、デプロイ保護ルール（承認など）を適用できます
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      # 1. 【🚨 変更点: scp-action を使用して、最新ファイルを VPS に確実に同期】
      - name: Sync Files to VPS (rsync via SCP)
        uses: appleboy/scp-action@v0.1.7 # scp-action に変更
        with:
          host: ${{ secrets.SSH_HOST_PROD }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          
          # ローカルの全ファイルをVPSのプロジェクトディレクトリへ転送
          source: "." 
          target: "/home/maya/shin-vps"
          # -r (再帰的) -t (タイムスタンプ保持) -z (圧縮) -q (静か) --delete (転送元にないファイルを削除)
          flags: "-rtzq --delete" 


      # 2. 【🚨 変更点: ssh-action を使ってコマンドのみ実行】
      - name: Deploy to Production VPS via SSH (Docker Compose Up)
        uses: appleboy/ssh-action@v1.0.3
        with:
          # 【削除】ファイル転送関連のパラメータ (src, target, sync) は scp-action に移動したため削除
          host: ${{ secrets.SSH_HOST_PROD }} 
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          
          # VPS上で実行するコマンド
          script: |
            # 1. Docker Composeファイルがあるディレクトリへ移動
            # ※ ファイルは scp-action によって最新に転送済みです
            cd /home/maya/shin-vps
            
            # 2. 本番環境のコンテナを再ビルドし、再起動
            # --build フラグが、転送された最新コードを使って自動で再ビルドを強制します。
            sudo docker compose -f docker-compose.prod.yml -p prod up -d --build --remove-orphans
            
            echo "Production deployment completed successfully."