/* ==========================================================
   Sidebar.module.css - Cyber-System Optimization
   ========================================================== */

.sidebar {
  width: 280px;
  background: #07070c; /* 深い闇 */
  padding: 24px 16px;
  border-right: 1px solid rgba(255, 255, 255, 0.03);
  height: fit-content;
  position: sticky;
  top: 80px;
  /* 縦幅の制御: ビューポートからヘッダー分を引いた高さに固定しスクロール可能に */
  max-height: calc(100vh - 100px);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
  overflow-x: hidden;
  scrollbar-gutter: stable;
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  z-index: 50;
}

/* --- 📡 PLATFORM MATRIX (グリッド・セレクター) --- */
.platformGrid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 6px;
  margin: 12px 0 24px 0;
}

.platLink {
  text-decoration: none;
}

.platBtn {
  padding: 10px 0;
  text-align: center;
  border: 1px solid rgba(255, 255, 255, 0.05);
  background: rgba(255, 255, 255, 0.01);
  border-radius: 1px;
  font-size: 9px;
  font-weight: 900;
  color: #555577;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  cursor: pointer;
  position: relative;
  overflow: hidden;
}

.platBtn:hover {
  color: #fff;
  background: rgba(255, 255, 255, 0.03);
  border-color: rgba(255, 255, 255, 0.2);
}

.platActive {
  color: #fff;
  background: var(--active-color);
  border-color: transparent;
  box-shadow: 0 0 15px var(--active-color);
}

/* --- 💾 HARDWARE ANALYSIS (スペックカード) --- */
.productSpecCard {
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.03) 0%, transparent 100%);
  border: 1px solid rgba(255, 255, 255, 0.05);
  border-left: 2px solid var(--site-color, #00f2ff);
  border-radius: 4px;
  padding: 16px 12px;
  margin: 12px 0 24px 0;
}

.specScoreBox {
  display: flex;
  align-items: center;
  gap: 12px;
  padding-bottom: 12px;
  margin-bottom: 12px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}

.scoreCircle {
  width: 45px;
  height: 45px;
  border: 2px solid rgba(255, 255, 255, 0.05);
  border-top: 2px solid var(--site-color);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.scoreValue {
  font-size: 1.2rem;
  font-weight: 900;
  font-family: 'Roboto Mono', monospace;
}

.scoreLabel {
  font-size: 0.55rem;
  color: #555577;
  letter-spacing: 0.2em;
  line-height: 1.2;
}

.miniSpecList {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.specRow {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 0.7rem;
}

.specRow small {
  color: #555577;
  font-weight: 700;
  text-transform: uppercase;
}

.specRow span {
  color: #bbbbcc;
  font-weight: 500;
  max-width: 65%;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* --- 🏷️ SECTION CONTROLS --- */
.sectionWrapper {
  width: 100%;
  margin-bottom: 4px;
}

.sectionTitle {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 0.65rem;
  font-weight: 800;
  letter-spacing: 0.2em;
  padding: 14px 4px;
  color: #888;
  text-transform: uppercase;
  transition: all 0.3s ease;
  user-select: none;
  border-bottom: 1px solid rgba(255, 255, 255, 0.02);
  cursor: pointer;
}

.sectionTitle:hover {
  background: rgba(255, 255, 255, 0.02);
  color: #fff;
}

.arrow {
  font-size: 0.5rem;
  opacity: 0.5;
  transition: transform 0.3s ease;
}

/* --- 🔗 LINK NODES & BADGES --- */
.accordionContent {
  list-style: none;
  padding: 8px 0;
  margin: 0;
  animation: scanIn 0.4s cubic-bezier(0.23, 1, 0.32, 1);
}

@keyframes scanIn {
  from { opacity: 0; transform: translateY(-5px); }
  to { opacity: 1; transform: translateY(0); }
}

.link {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 7px 10px;
  text-decoration: none;
  color: #777799;
  font-size: 0.75rem;
  border-radius: 2px;
  transition: all 0.2s;
  border-left: 1px solid transparent;
}

.link:hover {
  background: rgba(255, 255, 255, 0.04);
  color: #fff !important;
  border-left: 1px solid var(--site-color, #fff);
  padding-left: 14px;
}

.linkText {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  margin-right: 8px;
}

.badge {
  font-size: 0.6rem;
  font-family: 'Roboto Mono', monospace;
  background: rgba(0, 0, 0, 0.3);
  color: #555577;
  padding: 1px 5px;
  border-radius: 2px;
  border: 1px solid rgba(255, 255, 255, 0.05);
  flex-shrink: 0;
}

.link:hover .badge {
  color: var(--site-color, #fff);
  border-color: var(--site-color, rgba(255, 255, 255, 0.2));
}

/* --- 🕹️ SPECIAL HUB --- */
.specialLink {
  display: block;
  padding: 12px;
  text-decoration: none;
  color: #fff;
  font-size: 0.75rem;
  font-weight: 800;
  margin: 8px 0;
  position: relative;
  overflow: hidden;
}

.glitchText {
  position: relative;
  z-index: 1;
}

/* --- 💃 HIERARCHY (Actress / Source) --- */
.scrollArea {
  /* 内部スクロール領域の高さ調整 */
  max-height: 350px;
  overflow-y: auto;
  margin: 8px 0;
  padding-right: 4px;
}

.detailsGroup {
  margin-bottom: 2px;
}

.subCategoryLabel {
  list-style: none;
  font-size: 0.7rem;
  font-weight: 700;
  padding: 8px 10px;
  cursor: pointer;
  color: #9999aa;
  transition: 0.2s;
  display: flex;
  align-items: center;
  gap: 8px;
}

.subCategoryLabel::-webkit-details-marker { display: none; }

.subCategoryLabel:hover {
  color: #fff;
  background: rgba(255, 255, 255, 0.03);
}

.nestedList {
  list-style: none;
  padding: 4px 0 4px 12px;
  margin: 0 0 8px 10px;
  border-left: 1px solid rgba(255, 255, 255, 0.05);
}

/* --- 📄 INTEL LOGS --- */
.recentTitle {
  font-size: 0.7rem;
  line-height: 1.4;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  color: #8888aa;
}

.emptyLink {
  padding: 10px;
  font-size: 0.6rem;
  color: #444455;
  text-align: center;
  letter-spacing: 0.2em;
}

/* --- 📟 SYSTEM STATUS BAR --- */
.systemStatus {
  margin-top: auto; /* コンテンツが少なくても最下部に配置 */
  padding: 20px 10px 10px;
  border-top: 1px dashed rgba(255, 255, 255, 0.05);
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.statusLine {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 0.6rem;
  font-weight: 900;
  color: #555577;
  letter-spacing: 0.1em;
}

.statusDot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
}

.timestamp {
  font-family: 'Roboto Mono', monospace;
  font-size: 0.55rem;
  color: #333344;
}

/* --- 📜 CUSTOM SCROLLBAR --- */
.sidebar::-webkit-scrollbar { width: 4px; }
.sidebar::-webkit-scrollbar-track { background: transparent; }
.sidebar::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.03);
  border-radius: 10px;
}
.sidebar::-webkit-scrollbar-thumb:hover {
  background: var(--active-color, rgba(255, 255, 255, 0.1));
}

/* 内部スクロールエリア用 */
.scrollArea::-webkit-scrollbar { width: 2px; }
.scrollArea::-webkit-scrollbar-thumb { background: rgba(255, 255, 255, 0.1); }

/* --- 📱 MOBILE OPTIMIZATION --- */
@media (max-width: 1024px) {
  .sidebar {
    width: 100%;
    position: relative;
    top: 0;
    max-height: none;
    border-right: none;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    padding: 20px;
    height: auto;
  }
  .scrollArea { max-height: none; }
}