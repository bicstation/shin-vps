server {
    listen 80;
    server_name localhost;
    root /var/www/html;
    index index.php index.html index.htm;

    # 1. WordPressの標準的なパーマリンク設定
    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    # 2. PHPファイルの処理
    location ~ \.php$ {
        # 💡 docker-compose.ymlのサービス名と一致しているか再確認してください
        fastcgi_pass wordpress-v2:9000;
        fastcgi_index index.php;
        
        include fastcgi_params;
        
        # 必須のパスパラメータ
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        
        # ✅ 修正: サブディレクトリ運用ではないので /blog は不要
        fastcgi_param REQUEST_URI $request_uri;
        
        # 💡 SSLリダイレクトループを防ぐため、HTTPSであることを明示的に伝える
        fastcgi_param HTTPS on;
        
        # Hostヘッダー
        fastcgi_param HTTP_HOST $http_host; 
    }

    # 3. 静的ファイル（キャッシュ設定）
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|woff|woff2|ttf|eot|svg)$ {
        expires 30d;
        log_not_found off;
    }

    # 4. 隠しファイルへのアクセス拒否
    location ~ /\. {
        deny all;
    }
}