# -*- coding: utf-8 -*-
# /home/maya/dev/shin-vps/django/api/urls/master_urls.py

from django.urls import path

from api.views import master_views

# ==============================================================================
# 🚀 Namespace
# ==============================================================================

app_name = "master"

# ==============================================================================
# 🚀 URL Patterns
# ==============================================================================

urlpatterns = [

    # ==========================================================================
    # 👩 Actresses
    # ==========================================================================

    path(
        'actresses/',
        master_views.ActressListAPIView.as_view(),
        name='actress_list',
    ),

    # ==========================================================================
    # 🎭 Genres
    # ==========================================================================

    path(
        'genres/',
        master_views.GenreListAPIView.as_view(),
        name='genre_list',
    ),

    # ==========================================================================
    # 🏭 Makers
    # ==========================================================================

    path(
        'makers/',
        master_views.MakerListAPIView.as_view(),
        name='maker_list',
    ),

    # ==========================================================================
    # 🏷 Labels
    # ==========================================================================

    path(
        'labels/',
        master_views.LabelListAPIView.as_view(),
        name='label_list',
    ),

    # ==========================================================================
    # 🎬 Directors
    # ==========================================================================

    path(
        'directors/',
        master_views.DirectorListAPIView.as_view(),
        name='director_list',
    ),

    # ==========================================================================
    # 📚 Series
    # ==========================================================================

    path(
        'series/',
        master_views.SeriesListAPIView.as_view(),
        name='series_list',
    ),

    # ==========================================================================
    # ✍ Authors
    # ==========================================================================

    path(
        'authors/',
        master_views.AuthorListAPIView.as_view(),
        name='author_list',
    ),

    # ==========================================================================
    # 🧭 Navigation List
    # ==========================================================================

    path(
        'nav-list/',
        master_views.AdultNavListView.as_view(),
        name='navigation_list',
    ),
]