■ semantic frontend 方針確定共有

■ 結論

backend semantic engine の仕様が整理完了したため、
frontend も semantic attribute 構造へ移行開始してください

---

■ 基本方針

旧：
slug依存UI

↓

新：
type-driven semantic UI

---

■ attribute schema（正式）

{
id,
slug,
name,
type,

priority?,
score?,

icon?,
color?
}

※ keywords は backend semantic engine 内部用途

---

■ frontend 実装方針

① UI分岐を type ベースへ移行

旧：
if slug.startsWith('gpu-')

↓

新：
attribute.type === 'gpu'

---

② usage を semantic intent として扱う

usage はカテゴリではなく：

「何に使うPCか」

を表す semantic layer

例：
gaming
creator
mobile
ai
office

---

③ slug依存削減

slug用途：
・routing
・SEO
・semantic identifier

UI制御は type 優先へ移行

---

④ semantic attribute 共通化

以下を同一 attribute layer で統一：

・Finder
・Ranking
・Related
・Recommendation

---

■ backend 現在状態

TSV-driven semantic engine 化済

構成：

TSV
↓
DB sync
↓
semantic matcher
↓
usage scoring
↓
attribute attach

---

■ frontend 推奨移行順

① attribute type導入
② UI分岐変更
③ Finder semantic化
④ Related semantic化
⑤ Ranking semantic統合

---

■ 注意

slugベースの個別if文追加は禁止方向

新属性追加時は：
typeベースで吸収する構造へ移行

---

■ 目的

attribute を単なるタグではなく

「商品の意味データ」

として frontend / backend 共通運用する

---

■ ゴール

semantic engine と
semantic UI を

同一意味体系

として統合する

以上
