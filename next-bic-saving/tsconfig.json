{
  "extends": "./shared/tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"],
      /* ✅ shared 直下のファイルを直接参照できるようにします。
         これで @shared/lib/... や @shared/layout/... が物理パスと一致します。
      */
      "@shared/*": ["shared/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    /* ✅ shared フォルダを明示的に include することで、
       外部ディレクトリ扱い（externalDir）のエラーを防ぎます。
    */
    "shared/**/*.ts",
    "shared/**/*.tsx",
    "shared/**/*.css"
  ],
  "exclude": ["node_modules"]
}