@charset "utf-8";

/* ===============================================
   BICSTATION Modern Fleet Design Core
   Version: 2026.04 (Integrated)
   =============================================== */

/* 全体の基本設定 */
body {
    background-color: #f8f9fa;
    color: #333;
    font-family: 'Noto Sans JP', sans-serif;
    line-height: 1.6;
    margin: 0;
    padding: 0;
}

/* リンク設定 */
a { color: #0d6efd; text-decoration: none; transition: 0.3s; }
a:hover { color: #0a58ca; text-decoration: none; }

/* ヘッダー・ナビゲーション */
.navbar { border-bottom: 1px solid rgba(255,255,255,0.1); }

/* ネットワーク・ドロップダウン（拡張版） */
.dropdown-menu-fleet {
    min-width: 650px;
    padding: 20px;
    border-radius: 12px;
    border: none;
    background: #1a1d20;
    box-shadow: 0 15px 30px rgba(0,0,0,0.3);
}

.fleet-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
}

.fleet-header-nav {
    grid-column: span 3;
    color: #ffc107;
    font-weight: 900;
    text-align: center;
    border-bottom: 1px solid #333;
    padding-bottom: 10px;
    margin-bottom: 5px;
    letter-spacing: 1px;
}

.fleet-cat-label {
    grid-column: span 3;
    font-size: 11px;
    color: #ffc107;
    font-weight: bold;
    margin-top: 15px;
    text-transform: uppercase;
    border-left: 3px solid #ffc107;
    padding-left: 8px;
}

.fleet-item {
    background: #2b3035;
    padding: 10px;
    border-radius: 8px;
    display: flex;
    flex-direction: column;
    transition: 0.2s;
    border: 1px solid transparent;
}

.fleet-item:hover { background: #3d4246; border-color: #444; transform: translateY(-2px); }
.fleet-item b { color: #fff; font-size: 0.9rem; }
.fleet-item span { color: #888; font-size: 0.7rem; margin-top: 2px; }

/* 立体ナビゲーションボタン */
.guide-grid-card { 
    display: flex; flex-direction: column; justify-content: center;
    color: #fff !important; padding: 24px 20px; border-radius: 16px; 
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1); 
    height: 100%; position: relative; overflow: hidden;
    border: 1px solid rgba(255,255,255,0.1); box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}
.guide-grid-card:hover { transform: translateY(-8px); box-shadow: 0 20px 40px rgba(0,0,0,0.2); filter: brightness(1.1); }
.guide-grid-card::after {
    position: absolute; right: -10px; bottom: -15px; font-size: 4rem;
    opacity: 0.1; font-weight: 900; transform: rotate(-15deg); pointer-events: none;
}
.card-concierge { background: linear-gradient(135deg, #8a2be2, #4b0082); }
.card-concierge::after { content: "AI"; }
.card-bto { background: linear-gradient(135deg, #007bff, #003d7a); }
.card-bto::after { content: "PC"; }
.card-parts { background: linear-gradient(135deg, #28a745, #155724); }
.card-parts::after { content: "SET"; }
.card-money { background: linear-gradient(135deg, #ffc107, #e67e22); color: #fff !important; }
.card-money::after { content: "Acc"; }

/* 記事カード */
.article-card { border: none; border-radius: 15px; background: #fff; transition: 0.3s; overflow: hidden; }
.article-card:hover { transform: translateY(-5px); box-shadow: 0 15px 30px rgba(0,0,0,0.1) !important; }

/* サイドバー */
.side ul { list-style: none; padding: 0; margin: 0; }
.side li { padding: 8px 0; border-bottom: 1px solid #eee; }
.side li a { color: #444; font-size: 0.9rem; display: block; }
.side li a:hover { color: #0d6efd; padding-left: 5px; text-decoration: none; }

/* ページネーション */
.index-navigator { display: flex; justify-content: center; gap: 8px; list-style: none; padding: 0; }
.index-navigator a, .index-navigator span, .index-navigator .current-page {
    padding: 10px 18px; border-radius: 50px; background: #fff; border: 1px solid #dee2e6; color: #333; font-weight: bold;
}
.index-navigator .current-page { background: #0d6efd; color: #fff; border-color: #0d6efd; }

/* モバイル対応 */
@media (max-width: 768px) {
    .dropdown-menu-fleet { min-width: 100%; }
    .fleet-grid { grid-template-columns: repeat(2, 1fr); }
    .main-header { height: 200px; }
}