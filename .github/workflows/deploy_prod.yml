# ファイル名: .github/workflows/deploy_prod.yml

name: Production Deployment

on:
  # 本番環境へのデプロイは慎重に行うため、ステージングと同じく main ブランチへのプッシュをトリガーとします。
  push:
    branches:
      - main 

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production # 📘 環境名を指定することで、デプロイ保護ルール（承認など）を適用できます
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Production VPS via SSH (File Sync Mode)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST_PROD }} 
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          
          # 【🚨 追加】Actions Runnerでチェックアウトした最新コードをVPSに転送
          src: "." 
          target: "/home/maya/shin-vps"
          sync: true # rsyncを使用して差分のみ転送
          
          # VPS上で実行するコマンド
          script: |
            # 1. Docker Composeファイルがあるディレクトリへ移動
            # ※ ファイルは既にtargetディレクトリに転送されています
            cd /home/maya/shin-vps
            
            # 【🚨 削除】git pull は不要になりました（ファイル転送が代わりに行います）
            # git pull origin main 
            
            # 2. 本番環境のコンテナを再ビルドし、再起動
            # --build フラグが、転送された最新コードを使って自動で再ビルドを強制します。
            sudo docker compose -f docker-compose.prod.yml -p prod up -d --build --remove-orphans
            
            echo "Production deployment completed successfully."