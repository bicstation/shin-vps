# =====================================================================
# 💡 SHIN-VPS Nginx 統合・最終最適化定義 (2026 決定版)
# ---------------------------------------------------------------------
# 修正内容: 
# 1. b-avflash-host 用のバックエンド振り分けを追加
# 2. $http_host の正規表現を v3 コンテナ名に最適化
# =====================================================================

server {
    listen 80;
    server_name localhost;
    index index.php index.html index.htm;

    # 大きな画像やインポートを許可
    client_max_body_size 100M;

    # Docker内部のDNSサーバー (コンテナ名の解決に必須)
    resolver 127.0.0.11 valid=30s;

    # --- 0. HTTPS 判定ロジック ---
    set $fastcgi_https "off";
    if ($http_x_forwarded_proto = "https") {
        set $fastcgi_https "on";
    }

    # --- 1. 動的振り分けロジック (Hostヘッダーで判別) ---
    
    # 【デフォルト設定】 一般ブログ (Bicstation)
    set $wp_backend "wordpress-gen-v3";
    set $wp_root "/var/www/html/gen";

    # A. 節約ブログ (blog.bic-saving.com / b-saving-host)
    if ($http_host ~* (blog\.bic-saving\.com|b-saving-host|nginx-wp-saving)) {
        set $wp_backend "wordpress-saving-v3";
        set $wp_root "/var/www/html/saving";
    }

    # B. アダルトブログ (blog.tiper.live / b-tiper-host)
    if ($http_host ~* (blog\.tiper\.live|b-tiper-host|nginx-wp-adult)) {
        set $wp_backend "wordpress-adult-v3";
        set $wp_root "/var/www/html/adult";
    }

    # C. 一般ブログ (blog.bicstation.com / b-bicstation-host)
    # ※デフォルトと重なりますが、明示的に定義
    if ($http_host ~* (blog\.bicstation\.com|b-bicstation-host|nginx-wp-v3)) {
        set $wp_backend "wordpress-gen-v3";
        set $wp_root "/var/www/html/gen";
    }

    # D. AVFLASHブログ (b-avflash-host) 【←重要: 今回追加】
    if ($http_host ~* (blog\.avflash\.com|b-avflash-host|wordpress-avflash-v3)) {
        set $wp_backend "wordpress-avflash-v3";
        set $wp_root "/var/www/html/avflash";
    }

    # Nginx自身のドキュメントルートを設定
    root $wp_root;

    # --- 2. Google XML Sitemaps リライト ---
    rewrite ^/sitemap(-+([a-zA-Z0-9_-]+))?\.xml$ "/index.php?xml_sitemap=params=$2" last;
    rewrite ^/sitemap(-+([a-zA-Z0-9_-]+))?\.xml\.gz$ "/index.php?xml_sitemap=params=$2;zip=true" last;
    rewrite ^/sitemap(-+([a-zA-Z0-9_-]+))?\.html$ "/index.php?xml_sitemap=params=$2;html=true" last;
    rewrite ^/sitemap(-+([a-zA-Z0-9_-]+))?\.html\.gz$ "/index.php?xml_sitemap=params=$2;html=true;zip=true" last;

    # --- 3. メインルーティング ---
    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    # --- 4. PHP-FPM 連携 (ハイブリッド・パス対応) ---
    location ~ \.php$ {
        # 動的に決まった $wp_backend コンテナへ 9000番ポートで転送
        fastcgi_pass $wp_backend:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        
        # 【重要】SCRIPT_FILENAME の解決
        # PHPコンテナ側はすべて /var/www/html 直下にマウントされているため固定
        fastcgi_param SCRIPT_FILENAME /var/www/html$fastcgi_script_name;
        
        fastcgi_param REQUEST_URI $request_uri;
        fastcgi_param HTTP_HOST $http_host; 
        fastcgi_param HTTPS $fastcgi_https;
        
        fastcgi_read_timeout 300;
        fastcgi_intercept_errors on;
    }

    # --- 5. 静的ファイルキャッシュ ---
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|woff|woff2|ttf|eot|svg)$ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
        log_not_found off;
        access_log off;
    }

    # --- 6. セキュリティ ---
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}