{
  "extends": "./shared/tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"],
      /* ✅ ビルドエラー解決の鍵:
         @shared を shared フォルダそのものに向けることで、
         @shared/... も @shared/styles/... も一括で解決します。
      */
      "@shared/*": ["shared/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    /* ✅ shared フォルダ内のすべての TS/TSX ファイルと、
       globals.css を含む CSS ファイルをビルド対象として認識させます。
    */
    "shared/**/*.ts",
    "shared/**/*.tsx",
    "shared/**/*.css"
  ]
}